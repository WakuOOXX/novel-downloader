# 实施计划:书源「下拉」UI 重做 + 框架收尾(v2 定稿)

> 拟定:2026-09-09(凌晨)| v2 修订:00:46 拍板结论落定
> **执行状态:已全部完成 ✅(2026-09-09,commit d32c79b / c3ca645 / 文档随 v1.5 tag)**
> 项目:E:\book\bookdl(git 分支 new)
> 依据:app.py 现码 1331+ 行、legado/engine.py、docs/技术文档 v1.4、git log 至 f3c2197
> 颗粒度:**短期计划(≤7 天,按 4 个执行日排布)**

---

## 📍 现状结论:UI 背后的框架已经搭好(先回答你的疑问)

| 层 | 能力 | 落点(已核实) | 状态 |
|---|---|---|---|
| 状态模型 | 勾选文件集合 + 记忆 | `checked_files` / sel_state.`sources`(commit cd0b679) | ✅ 就位 |
| 加载合并 | 多文件逐文件加载、good 表逐文件信任、源数>5000 警示 | `_reload_all()`(app.py L414) | ⚠️ 缺跨文件去重(拍板⑤) |
| 勾选 UI | 复选菜单(Menubutton) | `_menu_populate`/`_pick_other_source`(L306/L372) | ⚠️ 按拍板重做 |
| 校验 | 勾选文件**逐文件**校验、文件级进度、写各自 good 表 | `start_verify`/`_do_verify`(L486/L512) | ✅ 就位 |
| 三域搜索 | 域下拉(自动/书名/作者/分类)+ 本地域过滤打分 | `var_domain`/`_relevant`/`_score_hit` | ✅ 就位(不动) |
| 书源分组 | 分组下拉 = **全部勾选文件合并后的全部分组** | `_set_groups(self.sources)`(L408) | ✅ 语义正确(验证即可) |
| 记忆 | 勾选/选项/域/校验记录统一写盘 | `_mem_flush`/`verify_dones` | ✅ 就位 |
| 同书分组 | 结果同书相邻 + 底色交替 + 【N源】 | `_gtag`/`merge_hits`(L724+) | ⚠️ 见风险① |
| 测试 | normalize 单测 | tests/test_normalize.py(11 条) | ⚠️ 未入 git |

**结论:框架层就位;剩余主战场 = 下拉 UI 重做 + 两个收尾风险 + 新增的去重功能。**

---

## 🚨 两个必须先决断的收尾风险(贯穿全程,不能丢线)

1. **engine.py 工作区未提交改动(回归风险)**:有人把 `search_sources` 的命中回调从「整批 merge 后 emit」改成「每源即时 on_hit(不 merge)」。后果:**搜索过程中**上屏行失去 `_group_key/_src_count` 注记 → 过程乱(同书不聚),等 `sres` 结束 `_fill_results` 再 merge 才恢复整齐。属"即时性 vs 过程分组"权衡,未决断、未测试、未提交。
2. **tests/ 未入 git、docs v1.4 之后缺更新。**

---

## ✅ 已拍板(2026-09-09 00:46,v2 落定)

| # | 决定 |
|---|---|
| 1 | 顶部名称**保留「书源文件」**(此前"书源全部"为误读,弃用);触发器显示「书源文件(N) ▼」 |
| 2 | 下拉列表 = **已加入的书源文件清单**(不再列目录全部 JSON) |
| 3 | 每行行尾 **✕ 删除图标 = 仅移出**(从清单移除,不删磁盘文件;想再用 → 重新"新加入") |
| 4 | 底部 **「+ 新加入书源…」**:弹选择面板 = ① shuyuan/ 内未加入的 JSON 勾选列表 ② 「浏览整机…」任意路径(复制进 shuyuan/) |
| 5 | 搜索域下拉、书源分组下拉**保持不动**;分组语义 = 全部勾选文件合并后的全部分组(现 `_set_groups` 已如此,验证+打勾) |
| 6 | 🆕 **合并重复书源**:`_reload_all` 合并多文件时按 `(bookSourceName, bookSourceUrl)` 去重,保留首个、优先保留 good 表来源;日志汇报去重数 |

> 备忘(不进本轮):若以后想要"临时停用某文件但不移出",再在行尾加禁用图标(P2)。

---

## 🗓️ 横向 by-day 框架(4 执行日)

```
┌──────────────┬─────────────────┬─────────────────┬─────────────────┬─────────────────┐
│              │ Day1 底账日      │ Day2 换皮日      │ Day3 手脚日       │ Day4 收官日       │
├──────────────┼─────────────────┼─────────────────┼─────────────────┼─────────────────┤
│ 主题          │ 数清家底再动刀   │ 弹出像分组一样   │ 增删随时听使唤   │ 记忆闭环收工     │
│ 目标(画面)    │ "框架到底搭没搭" │ "点下箭头弹出它" │ "每行能删、底部能加"│ "重启一切照旧"  │
│ 启动锚点(5分钟)│ 跑测试看红绿     │ 打开 app.py L179 │ 打开面板行模板   │ 改 sel_state 验证 │
│ 时间投入      │ 2-3h            │ 3-4h            │ 2-3h            │ 2-3h            │
│ 核心任务      │ ①engine 改动决断 │ ①触发器=书源文件 │ ①行尾 ✕ 仅移出   │ ①记忆回归全场景  │
│              │ ②tests 入 git    │   (N) ▼ + 自绘   │ ②「+ 新加入书源」 │ ②去重/分组回归  │
│              │ ③跑绿+基线 commit│   面板(已加入清单)│   (文件夹勾选+整机)│ ③文档 v1.5     │
│              │                 │ ③勾选联动 reload │ ③🆕合并重复书源  │ ④commit+tag     │
│ 产出物        │ 决断说明+绿测试  │ 可弹出的清单面板 │ 增删+去重闭环     │ v1.5 提交        │
│              │ +1 个 commit     │ +1 个 commit     │ +1 个 commit     │ +tag             │
│ 打分标准      │ 测试 11 条全绿   │ 弹出/移出/关闭   │ 增删各 1 路径     │ 手测清单全过     │
│              │ ;engine 改动有结论│ 零报错、计数正确 │ ;去重日志可核     │                 │
│ 高危预警      │ ⚠ 改一半就重构   │ ⚠ 沉迷像素级美化 │ ⚠ 移出误当删除    │ ⚠ 文档滞后       │
│ 破解          │ 只决断+提交      │ 功能达标即停     │ UI 文案写明"移出" │ 边做边更         │
└──────────────┴─────────────────┴─────────────────┴─────────────────┴─────────────────┘
```

---

## 🔧 各日详细执行指南

### Day1「底账日」—— 数清家底再动刀
- 🚀 启动锚点(零决策):终端 `cd E:/book/bookdl` → `python -m unittest discover -s tests -v`,红/绿结果贴日志。
- 任务:
  1. **engine.py 未提交改动决断**:验证「sres 结束 `_fill_results` 必 merge 刷新」(L1411 `merge_hits(dedupe_hits(...))`)成立 → 保留"每源即时上屏"(实时性好,过程短暂乱为代价);docstring 写明取舍;`_insert_hit_row` 无注记 fallback 确认无害。不成立则还原 emit。
  2. `git add tests/` 入库;README 补测试命令。
  3. 全绿 → 基线 commit:`chore: 引擎即时上屏取舍定案 + 单测入库`。
- 🔋 三档:满血=①②③;精简=只做①;保命=跑一次测试并把 engine 差异记进文档。
- 🌙 3 问:改了什么 / 卡哪 / 明天第一步。

### Day2「换皮日」—— 弹出"像分组一样"的清单面板
- 🚀 启动锚点:打开 app.py L179-191(书源文件行),触发器改「书源文件(N) ▼」。
- 任务:
  1. 自绘面板:Toplevel(overrideredirect、贴触发器下方、Esc/点击外部关闭),列表数据源改为 **`checked_files` 清单**(不再扫目录全部)。
  2. 顶栏:「书源文件」标题 + 计数;每行 = 文件名(+good 表状态小注)+ 行尾 ✕(Day3 接行为,先占位)。
  3. 勾选变化路径保留 `checked_files` + `_reload_all()` 现有链路;`_menu_populate` 的目录扫描逻辑**下沉**为「新加入面板」的数据源(Day3 用)。
- 产出:弹出/显示/关闭全通;1 commit。
- 🔋 三档:满血=面板全套;精简=触发器+只读清单弹出;保命=只改触发器文字。

### Day3「手脚日」—— 每行能删、底部能加、重复源合并
- 🚀 启动锚点:给面板行模板的 ✕ 按钮接上回调占位。
- 任务:
  1. 行尾 ✕:**仅移出**——`checked_files.remove` → `_reload_all()` → 记忆更新;日志明示「已移出(文件保留):xxx」。
  2. 底部「+ 新加入书源…」:小面板 = shuyuan/ 未加入 JSON 勾选列表 + 「浏览整机…」(filedialog 多选,复制进 shuyuan/,沿用 `_pick_other_source` 复制语义)→ 加入即 `_reload_all()`。
  3. 🆕 **合并重复书源**:`_reload_all` 合并时按 `(bookSourceName, bookSourceUrl)` 去重(首个保留,good 表来源优先);去重函数放 `legado/normalize.py`(`dedupe_sources`)便于单测;日志「勾选 N 文件 · 合并 M 源 · 去重 K 个重复源」;补 3 条单测(同名同 URL 去重 / 同名不同 URL 保留 / good 优先)。
  4. 回归:校验按钮(逐文件)、分组下拉(=合并后全部分组)、域下拉照常。
- 产出:增删+去重闭环 + 单测;1 commit。
- 🔋 三档:满血=①②③④;精简=①②+去重无单测;保命=只接「+ 新加入」。

### Day4「收官日」—— 记忆闭环收工
- 🚀 启动锚点:改 sel_state.json `sources` 增删一项,重启验证恢复。
- 任务:
  1. 记忆回归清单:清单文件集合、搜索域、模糊/相关勾选、fmt/mode、`verify_dones`——全场景重启恢复手测。
  2. 功能回归:移出→重启不出现;新加入→重启保留;重复源→去重日志与合并数一致;分组=全部勾选文件全部分组。
  3. 技术文档 v1.4 → v1.5(UI 面板交互、去重规则、engine 取舍、测试命令);README 同步。
  4. `git commit` + tag `v1.5`。
- 产出:v1.5 提交 + 手测清单全过。
- 🔋 三档:满血=①②③④;精简=①+④;保命=重启一次看清单在不在。

---

## SMART 检验(v2)

```
✅ Specific: 每日任务均落到文件/函数级(app.py L179/L414、normalize.dedupe_sources、engine.search_sources)
✅ Measurable: 4 commit + 单测(11+3)条全绿 + 手测清单逐项勾选
⚠️ Achievable: 风险提示——①自绘 Toplevel 面板坐标/失焦细节耗时(已给 Day2 精简档兜底);
   ②去重键 (bookSourceName, bookSourceUrl) 若个别源两字段皆空,需 fallback 保留并日志(Day3 处理);
   ③「仅移出」后文件仍在 shuyuan/,新加入面板的"未加入列表"会再见到它——这是预期行为(清单语义)
✅ Relevant: 每条对应拍板①-⑥或已核实风险
✅ Time-bound: 4 执行日 ×2-4h,每日独立 commit,中断不烂尾
```

## ✨ 方案亮点(v2)
1. 清单语义(加入/移出)与现有 `checked_files`/`sources` 记忆**天然同构**——只换 UI 皮,不动内核,记忆链路零迁移。
2. 去重做成 `normalize.dedupe_sources` 纯函数 + 单测,搜索/校验/分组自动受益。
3. 底账风险(engine 未提交改动、测试入库)与 UI 双线并进,不丢线。
4. 每日独立 commit,任何一天中断都不产生半成品。

## 📦 可导入
- 本文件即 MD(可入飞书/Notion/Obsidian);要 CSV 多维表格版随时说。
